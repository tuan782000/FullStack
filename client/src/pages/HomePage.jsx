import React from 'react';
import { Announcement, Navbar, Slider } from '../components';

const HomePage = () => {
    return (
        <div>
            <Announcement />
            <Navbar />
            <Slider />
        </div>
    );
};

export default HomePage;
